server:
  port: 9001
spring:
  #数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/root?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: root
#mybatis的相关配置
mybatis:
  #mapper的配置文件
  mapper-locations: classpath:mapper/*.xml
